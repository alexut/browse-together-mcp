# .env.example - Configuration reference

# Server settings
PORT=8888                # HTTP port for the service

# Browser settings
BROWSER_TYPE=chromium    # Browser type to use (chromium or firefox)
HEADLESS=false           # Run browser in headless mode
PROFILE_DIR=             # Custom profile directory (defaults to system config dir)

# Advanced browser settings
IGNORE_DEFAULT_ARGS='["--enable-automation"]'  # Default browser arguments to ignore as JSON array
BROWSER_ARGS='["--no-default-browser-check"]'  # Additional browser launch arguments as JSON array
BROWSER_OPTIONS='{}'     # Additional browser options as JSON string, e.g., '{"viewport":{"width":1280,"height":720}}'

# Security settings
BROWSER_API_TOKEN=      # Required API token for authentication (generate with scripts/generate-tokens.ts)

# Application behavior
APP_ENV=development      # development, testing, production (affects log level defaults)
LOG_LEVEL=debug          # debug, info, warn, error
